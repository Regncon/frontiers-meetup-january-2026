package slides

/*
Slide: LLM assistance (Good / Bad)

Intent:
- Capture a real-world observation: LLMs can be a superpower, but also amplify complexity.
- Keep it balanced: tooling maturity affects LLM usefulness.

Points:
- Bad: templ is newer and evolving; LLM support (examples, patterns, familiarity) was not great.
- Good: React has strong LLM support; LLMs generate React code well.
- Bad: That strength is a trap: it becomes easy to generate large amounts of React code quickly.

Asset:
- Add the generated image to: /static/slides/ai-chaos-developer.png
*/

templ LLMAssistanceGoodBad(isPresenter bool) {
	<div class="slide" style="display:flex; flex-direction:column; gap:1.5rem;">
		<h1 style="margin:0;">LLM assistance</h1>
		<div style="display:grid; grid-template-columns: 1.1fr 0.9fr; gap:1.5rem; align-items:start; flex:1;">
			<div>
				<ul style="margin-top: 0;">
					<li><strong>Bad:</strong> templ is newer and evolving — LLM support was not great</li>
					<li><strong>Good:</strong> React has strong LLM support</li>
					<li><strong>Bad:</strong> React has strong LLM support</li>
				</ul>
			</div>
			<div style="display:flex; align-items:center; justify-content:center;">
				<img
					src="/static/ai_developer.webp"
					alt="Obviously LLM-generated developer chaos illustration"
					style="max-width:100%; max-height:65vh; object-fit:contain; border-radius:16px;"
				/>
			</div>
		</div>
	</div>
	if isPresenter {
		@llmAssistanceGoodBadPresenterNotes()
	}
}

templ llmAssistanceGoodBadPresenterNotes() {
	<div class="presenter-notes">
		<ul>
			<li>We can’t have a presentation in 2026 without talking about AI.</li>
			<li>Templ llm support is terrible</li>
			<li>React llm support is great</li>
			<li>Good lle support leeds to vibe coding</li>
		</ul>
	</div>
}
